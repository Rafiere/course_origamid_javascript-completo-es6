/* O JavaScript é uma linguagem de programação interpretada. Ela não precisa ser compilada para ser executada. */

/* O JavaScript pode ser utilizado no front-end, para a manipulação do DOM, a comunicação assíncrona com o back-end e várias outras coisas, pode ser usado no back-end, para a comunicação com o banco de dados, a manipulação de arquivos e outros, através do Node, para jogos e várias outros modos. */

/* Abaixo, estamos criando uma função que recebe dois parâmetros. Ao término dessa função, ela retornará o valor da soma entre esses dois parâmetros recebidos. */

function somar(a, b) {
  return a + b;
}

somar(2, 4);
